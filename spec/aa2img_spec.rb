# frozen_string_literal: true

RSpec.describe Aa2img do
  it "has a version number" do
    expect(Aa2img::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
